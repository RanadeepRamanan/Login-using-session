public function username_check($str)
    {
        if ( !preg_match('/^[a-z \-]+$/i',$str) ) {
            $this->form_validation->set_message('username_check', 'It is not a valid Name');
        return false;
        } else {
            // echo "haii";die;
            return TRUE;
        }
    }
